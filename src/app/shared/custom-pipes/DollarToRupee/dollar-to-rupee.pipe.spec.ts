import { DollarToRupeePipe } from './dollar-to-rupee.pipe';

describe('DollarToRupeePipe', () => {
  it('create an instance', () => {
    const pipe = new DollarToRupeePipe();
    expect(pipe).toBeTruthy();
  });
});
